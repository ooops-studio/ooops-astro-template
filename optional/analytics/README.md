# Analytics Consent Module

Uses `@ooopsstudio/analytics-consent` and the Astro wrapper package for consent-aware analytics loading. The template keeps the visual skin local so every client project can restyle it.

All analytics env vars are optional. If the script URL or website id is missing, analytics is skipped.
