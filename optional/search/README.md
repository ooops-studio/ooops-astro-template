# Headless Search Module

Builds a static search index and provides small client-side helpers. It does not ship a fixed search UI; the Svelte example is only a reference implementation.

Run `node scripts/build-search-index.mjs` after adapting the content sources to the project.
