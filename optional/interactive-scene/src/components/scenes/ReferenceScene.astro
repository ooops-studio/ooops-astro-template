---
import '@ooopsstudio/scene-astro/base.css';
import InteractiveScene from '@ooopsstudio/scene-astro/InteractiveScene.astro';
import EditorBoundary from '../editor/EditorBoundary.astro';

type Props = {
  editorId?: string;
  backend?: 'auto' | 'webgl2';
  intensity?: number;
  pointerStrength?: number;
  distortion?: number;
  primaryColor?: string;
  secondaryColor?: string;
  quality?: 'low' | 'auto' | 'high';
  poster?: string;
  description?: string;
};

const {
  editorId,
  backend = 'auto',
  intensity = 1,
  pointerStrength = 0.8,
  distortion = 0.5,
  primaryColor = '#cd0208',
  secondaryColor = '#f1efef',
  quality = 'auto',
  poster = '/assets/scenes/reference-poster.png',
  description = 'A red distortion field follows pointer movement across a pale surface.'
} = Astro.props;
const scene = backend === 'webgl2' ? 'reference-scene-webgl2' : 'reference-scene';
---

<EditorBoundary editorId={editorId} manifestId="reference-scene">
  <InteractiveScene
    scene={scene}
    config={{intensity, pointerStrength, distortion, primaryColor, secondaryColor}}
    {quality}
    {poster}
    {description}
    aspectRatio="16 / 9"
  />
</EditorBoundary>

<script>
  import '../../scenes';
</script>
